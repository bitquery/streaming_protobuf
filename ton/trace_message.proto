syntax = "proto3";
package ton_messages;

import "ton/block_message.proto";

message Trace {
  ton_messages.BlockHeader Header = 1;
  ton_messages.Transaction Transaction = 2;
  repeated Trace Children = 3;
}

message TraceMessage {
  Chain Chain = 1;
  Trace Trace = 2;
}
